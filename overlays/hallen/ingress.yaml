apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flashcards
  annotations: {}
spec:
  ingressClassName: nginx
  rules:
    - host: flashcards.hallen.kaese.space
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: flashcards-backend
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flashcards-frontend
                port:
                  number: 80
